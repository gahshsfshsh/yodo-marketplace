# 🔧 Решение проблем с деплоем

## ❌ Проблема: "Connection closed" / "Connection reset"

### Причины:
1. SSH сервер блокирует множественные подключения
2. Firewall блокирует подключения
3. Неправильные права доступа

### Решение:

#### 1. Проверьте SSH подключение вручную:
```bash
ssh Tema@188.68.223.230
```

Если просит пароль - введите его.

#### 2. Если не подключается, проверьте:

**На вашем компьютере:**
```bash
# Проверка доступности сервера
ping 188.68.223.230

# Проверка SSH порта (22)
telnet 188.68.223.230 22
```

**На сервере (если есть доступ):**
```bash
# Проверка SSH сервиса
sudo systemctl status ssh

# Перезапуск SSH
sudo systemctl restart ssh

# Проверка firewall
sudo ufw status
sudo ufw allow 22
sudo ufw allow 3000
sudo ufw allow 3001
```

---

## ❌ Проблема: "No such file or directory"

### Причина:
Директории `/home/Tema/yodo/backend` и `/home/Tema/yodo/landing` не существуют

### Решение:

1. **Запустите скрипт настройки сервера:**
```bash
SETUP_SERVER.bat
```

2. **Или создайте директории вручную:**
```bash
ssh Tema@188.68.223.230

mkdir -p /home/Tema/yodo/backend
mkdir -p /home/Tema/yodo/landing

exit
```

---

## ❌ Проблема: "Permission denied"

### Причина:
Неправильный пароль или нет прав доступа

### Решение:

1. **Проверьте пароль:**
```bash
ssh Tema@188.68.223.230
# Введите правильный пароль
```

2. **Настройте SSH ключи (рекомендуется):**

**На вашем компьютере (Windows PowerShell):**
```powershell
# Генерируем SSH ключ (если еще нет)
ssh-keygen -t rsa -b 4096

# Копируем на сервер
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh Tema@188.68.223.230 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

После этого не будет нужен пароль!

---

## ❌ Проблема: Страница не загружается (порт 3000 или 3001)

### Причина:
Приложение не запущено или firewall блокирует порты

### Решение:

1. **Проверьте статус приложений:**
```bash
ssh Tema@188.68.223.230

# Проверка запущенных процессов
ps aux | grep -E "uvicorn|next"

# Проверка портов
sudo netstat -tulpn | grep -E "3000|3001"
```

2. **Откройте порты в firewall:**
```bash
sudo ufw allow 3000
sudo ufw allow 3001
sudo ufw reload
```

3. **Перезапустите приложения:**
```bash
# Backend (порт 3000)
cd /home/Tema/yodo/backend
pkill -f uvicorn
nohup python -m uvicorn main:app --host 0.0.0.0 --port 3000 > backend.log 2>&1 &

# Landing (порт 3001)
cd /home/Tema/yodo/landing
pkill -f "next start"
nohup npm start -- -p 3001 > landing.log 2>&1 &
```

---

## ✅ Пошаговое решение ВСЕХ проблем:

### Шаг 1: Настройте SSH
```bash
# Проверьте подключение
ssh Tema@188.68.223.230

# Если работает, переходите к шагу 2
```

### Шаг 2: Настройте сервер
```bash
# Запустите
SETUP_SERVER.bat
```

### Шаг 3: Задеплойте приложение
```bash
# Запустите
DEPLOY_FIXED.bat
```

### Шаг 4: Проверьте
- Backend: http://188.68.223.230:3000
- Landing: http://188.68.223.230:3001

---

## 📊 Полезные команды

### Проверка логов:
```bash
# Backend
ssh Tema@188.68.223.230 "tail -f /home/Tema/yodo/backend/backend.log"

# Landing
ssh Tema@188.68.223.230 "tail -f /home/Tema/yodo/landing/landing.log"
```

### Перезапуск:
```bash
# Backend
ssh Tema@188.68.223.230 "cd /home/Tema/yodo/backend && pkill -f uvicorn && nohup python -m uvicorn main:app --host 0.0.0.0 --port 3000 > backend.log 2>&1 &"

# Landing
ssh Tema@188.68.223.230 "cd /home/Tema/yodo/landing && pkill -f 'next start' && nohup npm start -- -p 3001 > landing.log 2>&1 &"
```

### Остановка:
```bash
# Все процессы
ssh Tema@188.68.223.230 "pkill -f uvicorn; pkill -f 'next start'"
```

---

## 🆘 Если ничего не помогает

### Вариант 1: Ручной деплой

1. Подключитесь к серверу:
```bash
ssh Tema@188.68.223.230
```

2. Создайте структуру:
```bash
mkdir -p /home/Tema/yodo/backend
mkdir -p /home/Tema/yodo/landing
```

3. На локальном компьютере соберите проект:
```bash
cd D:\yodo\landing
npm install --force
npm run build
```

4. Скопируйте файлы через FileZilla или WinSCP:
- Host: 188.68.223.230
- User: Tema
- Port: 22
- Скопируйте папку `landing` в `/home/Tema/yodo/`

5. На сервере запустите:
```bash
cd /home/Tema/yodo/landing
npm install --production
npm start -- -p 3001
```

### Вариант 2: Используйте PM2

На сервере:
```bash
# Установите PM2
sudo npm install -g pm2

# Backend
cd /home/Tema/yodo/backend
pm2 start "python -m uvicorn main:app --host 0.0.0.0 --port 3000" --name yodo-backend

# Landing
cd /home/Tema/yodo/landing
pm2 start npm --name "yodo-landing" -- start -- -p 3001

# Сохраните конфигурацию
pm2 save
pm2 startup
```

---

## 📞 Контакты

Если проблема не решается:
1. Проверьте все команды из этого файла
2. Сохраните логи ошибок
3. Проверьте доступ к серверу

**Удачи! 🚀**

